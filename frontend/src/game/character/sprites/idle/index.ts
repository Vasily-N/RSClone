import sprite from './index.png';
import { SpriteConfig } from '../../../entity';

const sc:SpriteConfig = {
  link: sprite,
  frameLength: 1 / 15,
  frameSize: 44,
  vertical: true,
  hasReverse: true,
};

export default sc;
