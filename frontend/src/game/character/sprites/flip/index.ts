import { SpriteConfig } from '../../../entity';

const sc:SpriteConfig = {
  link: '',
  frameLength: 1 / 20,
  frameSize: 37,
  vertical: true,
  hasReverse: true,
};

// todo: support reverse animation play

export default sc;
